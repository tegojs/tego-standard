# 租户隔离能力状态与后续计划

本文档是 `feat/platform-tenant-isolation` 分支的租户隔离权威文档。旧的早期问答稿、阶段计划和复核笔记已合并到本文档，后续以本文档记录当前状态、模块适配范围、未启用边界、测试缺口和下一步工作。

## 目标与边界

租户隔离能力面向低代码平台的「单套配置，多租户数据隔离」场景：

- 数据表定义、页面 schema、工作流定义、权限资源和插件配置尽量复用同一套元数据。
- 业务数据按租户隔离，默认由服务端强制处理，不依赖页面作者或业务插件手工追加 `tenantId` 条件。
- 用户通过当前租户上下文访问数据；平台管理员可以受控代入目标租户，并保留审计信息。
- 本分支采用行级隔离，不做多数据库、多部署实例或多应用强隔离。
- 首期主体能力落在标准模块仓库内，不以前置修改 `@tego/server` 核心为条件。

## 核心模型

平台统一使用以下概念：

| 概念 | 说明 |
| --- | --- |
| `tenant` | 租户实体，表示数据隔离边界。 |
| `tenantId` | 租户化业务记录的归属字段。 |
| `currentTenant` | 当前请求、页面或后台任务使用的租户上下文。 |
| `tenantScoped` | 仅当前租户可见的数据。 |
| `tenantInherited` | 当前租户及其子孙租户可见的数据。 |
| `shared` | 平台共享数据，不参与租户隔离。 |
| `legacyDataTenantIds` | 旧数据读可见配置，只影响读取，不放宽更新或删除。 |

集合租户语义统一通过 `collections.options.tenancy` 表达。`tenantScoped` 与 `tenantInherited` 集合使用 `tenantId` context field：

```ts
{
  type: 'context',
  name: 'tenantId',
  dataIndex: 'state.currentTenant.id',
  createOnly: true,
}
```

请求上下文标准字段：

- `X-Tenant-Id`
- `ctx.state.currentTenant`
- `ctx.state.currentTenantId`
- `ctx.state.currentTenantDescendantIds`
- `ctx.state.currentTenancyMode`
- `ctx.state.currentLegacyDataTenantIds`

## 当前已完成能力

### 租户模块

- 已新增 `packages/module-tenant` 前后端入口。
- 已支持 `tenants`、`tenantUsers`、用户默认租户、当前租户解析和租户切换接口。
- 已支持租户树：`parentId`、`parent`、`children`、`path`、子孙租户查询、移动校验、循环引用保护、删除非叶子节点保护、`path` 长度友好校验。
- `available-tenants` 当前规则是「用户直接归属租户 + enabled 子孙租户」，不向上包含祖先租户。
- `setCurrentTenant` 已支持按 `X-Tenant-Id`、默认租户和可访问租户解析当前上下文。
- 普通用户伪造不可访问租户会被拒绝；root 显式代入启用租户时保留 actor 审计上下文。
- `tenantResourceGuard` 已对标准资源 `list/get/count/export/create/update/destroy` 做强制租户过滤或写入注入。
- `update` 会移除请求体中的 `tenantId`，避免通过编辑改变数据归属。
- collection-manager 动态开启 `tenantScoped` / `tenantInherited` 时会补 `tenantId` 字段并同步表结构。
- 前端已提供导航栏租户切换入口、租户管理页面、集合模板租户配置和旧数据可见配置。

### ACL 与用户模块

- `module-acl` 已内建 `tenant` scope。
- 实际隔离兜底由资源层执行，ACL 主要补充权限语义和字段保护。
- `module-user` 的程序化 `defineCollection` 路径已覆盖 `tenantScoped` 与 `tenantInherited` 自动补 `tenantId`。
- 该自动补字段逻辑有 tenant 插件启用状态门禁：tenant 插件不存在或被禁用时不会注入 `tenantId`。

### 导入导出

- `plugin-action-import` 已在创建主记录时由服务端注入当前租户，导入文件中的 `tenantId` 不作为可信归属来源。
- 导入 transform 中 `o2o`、`o2m`、`m2o`、`m2m` 关联查询已传递当前请求 `context`，避免跨租户匹配同名关联记录。
- `plugin-action-export` 同步导出已按当前租户过滤，导出文件名带租户标识。
- 异步导出 worker 已接收完整 `tenantContext`，包含 `currentTenancyMode`、`currentTenantDescendantIds`、`currentLegacyDataTenantIds`，并在 worker 内重新应用过滤和租户化文件路径。

### 工作流

- workflow execution 已保存 `tenantId` 与可序列化 `tenantContext`。
- Processor 已提供 `getRepositoryContext()`，用于异步执行、重试和恢复时还原租户上下文。
- `QueryInstruction`、`SelectInstruction`、`UpdateInstruction`、`UpdateOrCreateInstruction`、`DestroyInstruction`、`AggregateInstruction` 已使用本地 helper 对直连 repository 路径追加租户过滤。
- general-action 同步和异步触发均传递 `{ httpContext: ctx }`，payload 查询已传 `context: ctx`。
- date-field schedule 已按触发记录构造租户上下文；`tenantInherited` 会查询 descendants。
- attachment upload 指令已把 execution 租户上下文写入内部上传请求的 `X-Tenant-Id`。

### 图表与报表

- `plugin-block-charts` 服务端查询已追加租户过滤。
- 查询前会剥离用户侧传入的 `tenantId` 过滤，再应用当前租户过滤，避免客户端覆盖隔离条件。
- 缓存 key 已纳入完整 `tenantScope`，包括 `tenancyMode`、descendants 和 `legacyDataTenantIds`。
- 已覆盖非默认 data source 下的租户集合解析。

### 文件与附件

- `module-file` 已将 `attachments` 标记为 `tenantScoped`。
- 上传路径和对象存储 key 已进入 `tenants/<tenantId>` 命名空间。
- 删除前查询附件时已传递当前 `context`。
- workflow 内部附件上传已传递租户 header。

### 审计日志

- `plugin-audit-logs` 已记录：
  - `tenantId`
  - `actorUserId`
  - `impersonatedTenantId`
  - `tenantContextSource`
  - `isTenantImpersonation`
- create/update/destroy hooks 已从 `options.context.state` 捕获租户和真实操作者信息。
- 前端集合配置已支持按 `tenantId` 筛选审计日志。
- 已补充 auditLogs 租户字段迁移。

### 客户端请求

- `packages/client` 的 `APIClient` 会在本地存在 `current_tenant_id` 时发送 `X-Tenant-Id` header。
- tenant 服务端模块未启用时没有中间件消费该 header，不会单独触发租户隔离逻辑。

### 迁移脚本

以下已有表已提供租户字段迁移，并包含幂等检查：

- workflow executions
- auditLogs
- attachments

## 租户模块未启用时的适配边界

当前复核结论如下：

- `module-user` 只在 `this.app.pm.get('tenant')?.enabled` 且集合声明 `tenantScoped/tenantInherited` 时自动注入 `tenantId`。
- `module-workflow` 使用本地 `helpers/tenant-context.ts`，运行时代码不静态导入 `@tachybase/module-tenant`；没有租户上下文时 no-op。
- `module-workflow` 与 `module-file` 的 tenant 依赖只用于开发和测试边界，不是运行时强加载路径。
- 前端租户菜单、租户管理页、集合模板中的 `tenancy/legacyDataTenantIds` 配置由 `module-tenant` 客户端插件注册；tenant 客户端插件未加载时不会暴露这些配置项。
- `APIClient` 发送 `X-Tenant-Id` 只是请求头行为；tenant 服务端模块未启用时没有对应消费逻辑。

## 已知测试缺口

以下测试仍为 skipped，属于上线前必须恢复或明确降级的缺口：

- `packages/module-file/src/server/__tests__/action.test.ts`
  - `upload file should be ok`
  - `upload to storage which is not default`
  - `should isolate attachments by current tenant`
  - `should store attachment files under tenant-specific paths`

## 剩余工作

### 1. 恢复 skipped 测试

优先恢复 `module-file` 的 4 个 skipped 用例。workflow date-field schedule 的 2 个租户上下文测试已恢复（移除硬编码 sleep，改用条件等待；调整记录创建顺序避免 SQLITE_BUSY 锁竞争）。当前代码路径已基本补齐，主要风险在测试装配和定时测试稳定性。

### 2. 统一隔离入口收口

当前强制隔离主要由 `module-tenant` 的资源中间件和各模块显式 helper 兜底。后续如要进一步降低插件漏接风险，应评估把标准资源访问隔离能力继续向 `module-data-source` 或更底层收口。

### 3. 历史数据归类迁移工具

`legacyDataTenantIds` 只是旧数据读时兼容策略，不等同于完整迁移工具。后续需要补充：

- 存量数据归类扫描工具
- 按集合批量回填 `tenantId`
- dry-run 报告
- 可回滚记录
- 灰度开关

### 4. 审计告警与排障

审计日志字段已经具备基础，但告警和排障工具仍未完成：

- 无租户上下文访问 tenant-enabled 集合的告警
- 普通用户跨租户访问尝试告警
- 异常大批量导出告警
- 按 tenant tag 聚合 trace/log/metric

### 5. 上线前验证与压测

上线前仍需覆盖：

- 双租户并发 CRUD
- `tenantScoped` 与 `tenantInherited` 的读写边界
- 导入关联查询
- 同步与异步导出
- 工作流同步、异步、重试、resume、date-field schedule
- 图表查询和缓存
- 附件上传、删除和路径隔离
- 平台管理员代入与审计
- 真实数据库迁移验证
- 多租户并发压测与慢查询分析

## 重要实现原则

- 客户端传入的 `tenantId` 默认不可信。
- `legacyDataTenantIds` 只放宽读取，不放宽更新和删除。
- 平台管理员代入只改变 effective tenant，不覆盖 `currentUser`。
- 页面 schema 和插件配置尽量共享；运行时数据访问由服务端按当前租户自动筛分。
- 共享字典、平台配置、SQL / view 集合等不应被误加租户字段或租户过滤。

## 相关文档

- `docs/superpowers/tego-core-test-infrastructure-migration.md`：核心测试基础设施迁移计划，属于测试工具链收口，不属于租户业务能力本身。
