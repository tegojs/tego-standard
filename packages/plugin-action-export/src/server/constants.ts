export const EXPORT_LENGTH_MAX = +ctx.tego.environment.getVariables().EXPORT_LENGTH_MAX || 2000;
export const EXPORT_WORKER_PAGESIZE = +ctx.tego.environment.getVariables().EXPORT_WORKER_PAGESIZE || 1000;
